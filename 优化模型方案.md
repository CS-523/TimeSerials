# 优化模型方案：输入一段 x，优化未来 x3/x4/x6/x8 使 y4 最大

> 对应 README 任务 4："输入一段时间数据，优化未来一段时间的 x3、x4、x6、x8；使 y4 或者 y 最大。"
> 本文档经讨论后确定：**目标 = 仅最大化 y4**（不含标量 Y / YHead）。

---

## 1. 背景

仓库里**曾经有两套优化器，但都被移除**：

| 优化器 | 位置 | 状态 |
|---|---|---|
| 梯度 + CEM（基于 PathIntegratorForecaster） | `src/optimize.py` | 在 `4195d11` 被掏空成占位（预测模型去掉了 y_head） |
| 可微 L-BFGS Pareto MPC | `src_control/optimization/mpc_optimizer.py` | 在 `ea7cc5b` 被整条删除 |

但**预测能力其实都还在** `src_control/` 里，且都有训练好的 checkpoint：

| 模型 | 类 / checkpoint | 作用 | 可微？ |
|---|---|---|---|
| x 预测器（默认轨迹） | `SS_NN_Hybrid(dim_u=8,dim_y=8)` / `checkpoints/x_forecast_best.pt` | `forecast(ctx,H)` → 未来 x1–x8（自回归） | 是 |
| y4 预测器（奖励） | `SS_NN_Hybrid(dim_u=8,dim_y=4)` / `checkpoints/ss_nn_best.pt` 的 `model` 键 | `forward(u)` → y1–y4，`y4=u[...,3]` | 是 |

两者共用 `data/processed/scalers.npz`（`x_mean`/`x_scale`）的标准化空间，可以直接串联。

---

## 2. 关键澄清：为什么那 4 个变量是"自由"的

**"自由"指"可控（能被操作员主动设定）"，不是"可预测/不可预测"。**

所有 8 个 x 的未来值都可以被 x 预测器预测出来（这是"不干预"的默认轨迹）。但优化只允许在**你实际能动手控制的变量**上做搜索：

- **可控（决策变量）：x3、x4、x6、x8**
  - REPORT 中：x3/x4 是"控制自由度"、x6 是"高位产能/反应浓度"、x8 是可控累计投料。
- **不可控（随预测走，优化时不改动）：x1、x2、x5、x7**
  - 外生/环境变量（如 pH、温度）和累计结果量，操作员不能直接设定。

因此完整闭环：

```
过去窗口 ──x预测器──▶ 未来 x1..x8 默认轨迹
                         │
        非自由维(x1,x2,x5,x7) 固定在预测值（不可控，只能预测）
        自由维  (x3,x4,x6,x8) 当搜索变量（初值=预测值，加物理上下界）
                         │
                    组装完整未来 x ──y4预测器──▶ 最大化未来 y4
```

即：**非自由变量由预测器预测；自由变量在预测值附近做有界搜索。**

---

## 3. 设计

### 3.1 数据流（每个样本）

```
输入: 过去窗口 x_ctx  (C,8) 标准化
  │
  ├─ x 预测器.forecast(x_ctx, H)          → x_fut_default (H,8)    [默认轨迹]
  │
  ├─ 决策变量 x_ctrl (H,4)  = 取 x_fut_default 的 [x3,x4,x6,x8] 列，
  │      requires_grad=True，初值=预测值，上下界=物理范围
  │
  ├─ 每步: 组装 future_x (H,8) = 默认轨迹，用 x_ctrl 覆盖第 2/3/5/7 列
  │        u = cat([x_ctx, future_x], dim=0)  → (C+H, 8)
  │
  └─ y4 预测器.forward(u[None])           → y (1, C+H, 4)
          目标 = max mean( y[0, C:, 3] )     # 未来 H 步 y4 的均值
```

### 3.2 核心机制

1. **决策变量索引**：`DECISION_IDX = (2,3,5,7)`（x3,x4,x6,x8）；`FIXED_IDX = (0,1,4,6)`（x1,x2,x5,x7）。
2. **优化器**：投影梯度上升。用 `torch.optim.LBFGS`（或 Adam）对 `x_ctrl (H,4)` 做多起点优化，每个 step 后 `clamp` 到标准化上下界（投影）。
   - 起点 1 = 预测默认值（"延续当前策略"），起点 2..N = 界内均匀随机（多起点避免局部最优）。
3. **物理上下界**（原始空间，来自被删 `OptConfig`/`VAR_RANGES`）：`x3∈(0,110)`、`x4∈(26,36)`、`x6∈(5000,50000)`、`x8∈(0,1500)`。
4. **标准化空间一致性**（修复被删代码的 bug——原代码把原始界直接套在标准化输入上）：
   所有优化在标准化空间进行，物理界换算成标准化界
   `lo_norm=(lo_raw−x_mean[c])/x_scale[c]`、`hi_norm=(hi_raw−x_mean[c])/x_scale[c]`；
   结果再反标准化输出原始值。
5. **目标**：`loss = -y4_future.mean()`（H 固定时 sum 与 mean 等价，用 mean 更稳）。
6. **控制 effort 正则（可选）**：`loss = -y4.mean() + λ·‖x_ctrl − x_default‖²`，惩罚控制量相对默认轨迹的偏离，避免"瞬时大幅跳变"在物理上不可达。默认 `λ=0`（关闭），由 `--effort-penalty` 调节。

---

## 4. 实现步骤

### 4.1 核心优化器 — 新建 `src_control/optimization/optimizer.py`

复用 `src_control/models/state_space_nn.py` 的 `SS_NN_Hybrid`（`forward` + `forecast`），实现 `maximize_y4(x_ctx, ...)`：

- 入参：`x_ctx (C,8)`、两个模型、标准化上下界、`H`、`n_starts`、`max_iter`、`lr`、`effort_penalty`（默认 0）。
- 逻辑：按 3.1 数据流组装 `u`，返回 `(best_x_ctrl_norm, best_y4, history)`。
- 关键点：`x_ctrl` 是唯一 `requires_grad=True` 的叶子；`x_ctx` 与非自由维用 `.detach()`/`no_grad` 固定。
- 参考（不照抄）：`git show ea7cc5b^:src_control/optimization/mpc_optimizer.py`（L-BFGS + clamp 投影的写法）。

### 4.2 CLI 驱动 — 新建 `scripts_control/04_optimize.py`

- 加载两个 checkpoint + `scalers.npz`，从 `load_processed(test.npz)` 取一个/多个样本的 context。
- 调 `maximize_y4`，输出：
  - `results/optimization/optimized_vs_baseline_{i}.png`：默认轨迹 vs 优化后 x3/x4/x6/x8（反标准化）；
  - `results/optimization/optimize_metrics.json`：每个样本 baseline y4 均值 vs 优化后 y4 均值、提升幅度、是否越界。
- 参数：`--context 32 --horizon 16 --n-starts 5 --max-iter 200 --lr 0.05`（对齐被删 `OPT_*` 配置），外加 `--effort-penalty 0.0`。
- **批量评估开关 `--all-test`**：遍历整个 test 集，`optimize_metrics.json` 汇总所有样本的 baseline/optimized y4 与提升分布（而不只是单个样本），便于看整体效果分布。

### 4.3 恢复配置常量（可选）— `src_control/config.py`

若已删，补回：`DECISION_COLS=(x3,x4,x6,x8)`、`FIXED_INPUT_COLS=(x1,x2,x5,x7)`、`VAR_RANGES` 及 `OPT_*`（`OPT_HORIZON=16` 等），供 `04_optimize.py` 引用。先读当前 `config.py` 确认哪些还在。

### 4.4 文档 — `scripts_control/README.md` / `实验日志.md`

补一句 `04_optimize.py` 的用法与结果结论（去掉之前"优化暂停"的表述）。

---

## 5. 关键文件与复用点

- 复用模型类：`src_control/models/state_space_nn.py`（`SS_NN_Hybrid.forward` / `.forecast`，均已可微）。
- 复用数据/缩放：`src_control/preprocess.py` 的 `load_processed`；`data/processed/scalers.npz` 的 `x_mean`/`x_scale`。
- 复用配置：`src_control/config.py` 的 `get_config`/`resolve_paths`。
- 参考旧实现（git 可恢复）：`git show ea7cc5b^:src_control/optimization/mpc_optimizer.py`、`git show ea7cc5b^:scripts_control/04_optimize.py`、`git show 5d3815c:src/optimize.py`。
- 现有占位：`src/optimize.py`（本轮不动，保持 src/ 与 src_control/ 两条管线独立）。

---

## 6. 验证（端到端）

1. **冒烟**：`python -m scripts_control.04_optimize.py --n-samples 1 --horizon 16`，确认两个模型加载成功、优化不报错、输出 PNG + JSON。
2. **目标上升**：对每个测试样本，`optimized_y4_mean ≥ baseline_y4_mean`（初值本身是可行点，梯度上升不应变差；若变差说明投影/标准化 bug）。
3. **边界**：断言优化后的 x3/x4/x6/x8 反标准化后落在物理界内。
4. **可视化核对**：PNG 里优化轨迹与默认轨迹合理（不出现跳变/越界）；y4 曲线提升。
5. **对照**：抽查 1 个样本，把优化结果与被删 MPC（`git show` 恢复后临时跑）对齐，确认方向一致、且修复了标准化/原始空间混用。
6. **批量评估**：`--all-test` 跑全 test 集，看 y4 提升的分布（中位数/分位数），确认多数样本为正提升；调 `--effort-penalty` 观察控制轨迹是否更平滑（跳变减小）。
