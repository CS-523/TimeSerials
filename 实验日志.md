# 实验日志

## 2026-08-15 — 多组预测模型对比（LSTM vs PathInt × shared / group_head）

### 实验配置

- **数据**：5 组实验（目录 `1`~`5`），每组若干 CSV 时序，8 个过程变量 x1–x8，30 分钟采样。
- **切分**：`split_experiments_groupwise`（按实验 ID 分组切分），`seed=42`。
- **标准化**：`Scaler.fit(train_exps)`，对 groupwise train 集算 8 维 mean/std（`(x - mean) / std`）。
- **模型**：
  - LSTM 骨干：`hidden=128, num_layers=2, dropout=0.1`，LR `2e-3`。
  - PathInt 骨干：`dim_state=128, hidden=128`，LR `5e-4`。
- **训练**：`epochs=60`，前 `tf_epochs=10` 个 epoch 用 Teacher Forcing，之后纯自回归 rollout；loss = MSE(x)。
- **组策略**：
  - `shared`：全共享（单一模型）；
  - `group_head`：共享骨干 + 每组一个 FiLM 头 (γ_g, β_g) ∈ R^8；
  - `independent`（已移除，不参与本次对比）。

### 结果

**汇总（test 集，原始空间 RMSE）**

| Model | Backbone | Mode | Params | RMSE(x) 标准化 | RMSE(x) 原始 | G1 | G2 | G3 | G4 | G5 |
|---|---|---|---|---|---|---|---|---|---|---|
| LSTM shared | lstm | shared | 266,440 | 0.2768 | 1565.9 | 550.9 | 558.9 | 572.8 | 526.5 | 568.4 |
| LSTM group_head | lstm | group_head | 266,520 | 0.2784 | 1575.1 | 558.0 | 557.6 | 563.8 | 560.2 | 540.3 |
| PathInt shared | pathint | shared | 2,249,224 | 0.2426 | 1372.5 | 490.9 | 479.1 | 496.2 | 486.7 | 469.7 |
| PathInt group_head | pathint | group_head | 2,249,304 | 0.2479 | 1402.9 | 507.7 | 506.3 | 516.9 | 488.2 | 450.1 |

**自回归 AR vs Teacher Forcing TF（整体 RMSE，原始空间）**

| Model | AR | TF | 漂移(AR−TF) | 漂移占比 |
|---|---|---|---|---|
| LSTM shared | 1565.9 | 1197.0 | 368.9 | 23.6% |
| LSTM group_head | 1575.1 | 1222.2 | 352.8 | 22.4% |
| PathInt shared | 1372.5 | 912.6 | 459.8 | 33.5% |
| PathInt group_head | 1402.9 | 924.8 | 478.1 | 34.1% |

**group_head vs shared（组适配头是否有增益）**

| Backbone | shared | group_head | 提升 |
|---|---|---|---|
| LSTM | 1565.9 | 1575.1 | −0.6% |
| PathInt | 1372.5 | 1402.9 | −2.2% |

**group_head vs independent（数据效率）**

`independent` 模式已移除，无此对比。

**LSTM vs PathInt（同模式）**

| Mode | LSTM | PathInt | 差异 |
|---|---|---|---|
| shared | 1565.9 | 1372.5 | −12.4%（PathInt 优） |
| group_head | 1575.1 | 1402.9 | −10.9%（PathInt 优） |

**小结**

1. **PathInt 明显优于 LSTM**：`shared` 低 12.4%，`group_head` 低 10.9%。
2. **group_head 略差于 shared**：LSTM −0.6%、PathInt −2.2%，说明 5 组共享特征已足够，加 FiLM 组适配头未带来增益。
3. **AR→TF 漂移 PathInt 更大**（~33–34%）vs LSTM（~22–24%）：自回归 rollout 下 PathInt 的误差累积更明显。
4. 分组成绩整体接近，PathInt 在 G5 上最好（shared 469.7 / group_head 450.1）。

### 产物

- `src/model_out/compare_metrics.json`（test） / `_train.json` / `_tf.json`
- `src/model_out/compare_bars_lstm.png` / `compare_bars_pathint.png` / `compare_bars_all.png` / `compare_bars_all_train.png` / `compare_bars_tf_vs_ar.png`

### 本次代码改动

- **`compare_multigroup.py`**：checkpoint 增加 `x_scaler` 字段（与 `train_multigroup.py` 对齐，使 ckpt 自包含）。旧的 ckpt 仍无 scaler，`visualize.py` 会走 refit 兜底（`Scaler.fit(train_exps)`，与训练时一致，确定性、无误差）。
- **`visualize.py`**：预测折线图样式调整——truth 线加粗（`linewidth=2.5`、圆点 `markersize=4`），pred 线变细 + 半透明（`linewidth=1.0`、`alpha=0.7`），避免红 pred 盖住绿 truth。

### 已知问题

- **预测图只显示前 40 点**：`visualize.py` 写死 `in_len=24`（输入）+ `T_out=16`（预测）= 40 点；完整轨迹可达 300+ 点。可通过 `--pred-start` 看后段，但默认仅前 40 步。
- **x7 预测明显偏差**：实测误差 ~22110，而 truth 范围仅 831~7912，会把预测图 y 轴撑爆、压缩 truth/history。属模型对 x7 未学好，非绘图问题。

---

## 2026-08-21 — 任务 1 `compare_multigroup.py` 数据处理流程详解

> 本节按 `src/compare_multigroup.py` 的实际调用顺序，逐阶段说明**原始 CSV → 训练样本**的转换过程，对应源码行号用 `Lxx` 标出。涉及辅助模块 `data_loader.py`（读 CSV / 清洗 / 滑窗）、`train_multigroup.py`（groupwise 切分 / Dataset / collate）。

### 1. 加载原始 CSV（[compare_multigroup.py:324](src/compare_multigroup.py#L324) → `data_loader.load_all`）

- 遍历根目录下的 `1/` `2/` `3/` `4/` `5/` 五个子目录，按文件名排序后逐个读入每个 CSV。
- 每个 CSV 视为**一个实验**，共 171 条（36+36+32+43+24）。
- 单文件读取 `_read_one_csv`（[data_loader.py:36](src/data_loader.py#L36)）：
  - 全部以 `str` 读入，避免 NA 自动转换；
  - 丢掉 `datime=NaT` 的纯 Y 尾行；
  - 把 `x1..x8 / y1..y4 / 周期 / Y` 转数值（`pd.to_numeric(..., errors="coerce")`）。
- 用 `_extract_Y`（[data_loader.py:51](src/data_loader.py#L51)）从 Y 列抽出该实验的**终值标量**。
- 最终每个 Experiment 含：文件路径、`group`（目录名 1~5）、对齐+清洗后的时序 `df`、终值 `Y`。

### 2. 周期对齐 `_align_to_period`（[data_loader.py:61](src/data_loader.py#L61)）

> 解决"x 行"和"y 行"在不同物理行的拆分。

- 原始 CSV 中"整点"行（`09:00:00, 09:30:00, ...`）含 x1–x8，"15 秒"行（`09:45:12.530`）通常 x 全空、含 y1–y4。
- 先把 y-only 行按 `周期` 分组取首条，得到 `y_map: 周期 → {y1,y2,y3,y4}`。
- 取所有 x 非空行作为主表（`x_rows`），按 `周期` 关联 `y_map`（`merge` 后用 `fillna` 兜底空值），再按 `周期` 排序重置索引。
- 结果：每个 `周期` 一行，同时含 x1–x8 与 y1–y4（y 可能为 NaN，训练时用掩码）。

### 3. 异常清洗 `_clean_anomalies`（[data_loader.py:96](src/data_loader.py#L96)）

- **IQR 裁剪**（每列独立，3×IQR 边界），把 x 列异常值夹回 `[Q1-3IQR, Q3+3IQR]`。
- **线性内插** `x` 缺失值（`interpolate(method="linear", limit_direction="both")`）。
- y 列的缺失**保留为 NaN**（后续 y 头/任务 2 用掩码训练）。

### 4. groupwise 切分 `split_experiments_groupwise`（[compare_multigroup.py:325](src/compare_multigroup.py#L325)）

> 与 `data_loader.split_experiments`（全局随机）不同，**任务 1 用 groupwise**，保证每个组在 train/val/test 三集合都有样本（便于逐组评估）。

- 流程（[train_multigroup.py:58](src/train_multigroup.py#L58)）：对 `g ∈ {1,2,3,4,5}`，分别按 `seed=42` 随机抽 70%/10%/20% → 合并成全局 train/val/test。
- 打印每组在三个集合的实验数（[compare_multigroup.py:327](src/compare_multigroup.py#L327)）。
- 若某组实验数 < 3，会强制 `n_train=1, n_val=0`，避免空集。

### 5. 标准化 `Scaler.fit`（[compare_multigroup.py:333](src/compare_multigroup.py#L333)）

- **只对 x1–x8** 标准化：把 `train_exps` 所有 df 的 x 列按行拼起来 → `mean ∈ R^8, std ∈ R^8`（`std` 加 `1e-6` 防 0 除）。
- 公式 `(x - mean) / std`（`Scaler.transform`）。
- **只在 train 集上 fit**（防数据泄露），val/test 沿用同一 scaler。
- 打印 `x_scaler.std.mean()`（[compare_multigroup.py:334](src/compare_multigroup.py#L334)）作为后续原始空间 RMSE 换算的标尺。
- 同步落盘到 `src/model_out/scalers_{mode}.npz`（`x_mean / x_std`）以及 checkpoint 的 `x_scaler` 字段。

### 6. 滑窗采样 `sample_windows`（[compare_multigroup.py:336](src/compare_multigroup.py#L336)）

- 入口参数：每个实验至少 `min_total=24` 周期才参与采样；每条实验采 `n // 30`（至少 1）个窗口。
- 默认 `in_lens=(16,20,24,32), out_lens=(6,8,12,16)`（[data_loader.py:169](src/data_loader.py#L169)）。
- 对每个窗口随机选 `(in_len, out_len)` 与 `start`，要求 `start + in_len + out_len ≤ n`。
- 输出 `WindowSample`（[data_loader.py:155](src/data_loader.py#L155)）：`x_in, y_in, x_out, y_out`（均为原始空间 `float32`），`exp_id, start, in_len, out_len, Y`。
- 三集合使用不同 `rng_seed`（`seed / seed+1 / seed+2`，[compare_multigroup.py:336-338](src/compare_multigroup.py#L336-L338)），保证滑窗不重叠随机性。

### 7. 挂 group_id（[compare_multigroup.py:340](src/compare_multigroup.py#L340)）

- `attach` 把 `WindowSample.exp_id` 映射到所属 Experiment 的 group（`int(group)-1` ∈ {0,1,2,3,4}）。
- **任务 1 只有 `group_head` 模式会用到**（输入 FiLM 头），`shared` 模式不读取 `group_ids`。

### 8. Dataset / Collate

- `WindowXGDataset`（[train_multigroup.py:83](src/train_multigroup.py#L83)）：`__getitem__` 时调用 `x_scaler.transform` 把 `x_in / x_out` 标准化为张量。
- `pad_collate_xg`（[train_multigroup.py:105](src/train_multigroup.py#L105)）：把变长 `(in_len, out_len)` 样本 pad 到 batch 内最大长度，输出 `x_in(B,T_in,8) / x_out(B,T_out,8) / in_lens / out_lens / group_ids(B,)`。

### 9. 训练阶段的 TF / AR 切换（[compare_multigroup.py:144-193](src/compare_multigroup.py#L144-L193)）

- 数据进入模型后，TF 段（前 `tf_epochs=10` epoch）：把真实 `x_out` 喂入 rollout，forward 一步外推用 `x_out[:, t]` 作为下一步输入；
- AR 段（剩余 epoch）：纯自回归，每步用上一步预测作为输入；
- Loss = 变长对齐的 `MSE(pred_x, x_out)`，梯度裁剪 `clip_grad_norm_(1.0)`，Adam + CosineAnnealing。

### 10. 评估口径

- 整体 RMSE：把所有样本、所有 8 维、所有时间步的平方误差聚合计入 `sse_x / cnt_x`（[train_multigroup.py:226](src/train_multigroup.py#L226)）。
- per-group RMSE：按 `group_ids[i]` 分桶（[train_multigroup.py:235](src/train_multigroup.py#L235)）。
- 原始空间 RMSE = `norm_rmse × x_scaler.std.mean()`（[train_multigroup.py:230](src/train_multigroup.py#L230)），作为跨变量平均量纲。
- AR vs TF：同一组 ckpt，分别用纯自回归 / 一步外推得到两份 RMSE，比较**累积漂移**（[compare_multigroup.py:537](src/compare_multigroup.py#L537)）。

### 数据流总图

```
原始 CSV (171 个)
   │ load_all → _align_to_period → _clean_anomalies
   ▼
Experiment 列表 (含 group / df / Y)
   │ split_experiments_groupwise (按组 0.7/0.1/0.2)
   ├──► train_exps ──┐
   ├──► val_exps   ──┤
   └──► test_exps  ──┘
        │
        │ Scaler.fit(train_exps)  →  8 维 mean/std
        │
        │ sample_windows(in_lens=(16,20,24,32), out_lens=(6,8,12,16))
        ▼
   WindowSample (exp_id / start / in_len / out_len / x_in / x_out / group)
        │
        │ WindowXGDataset.transform  →  (x-mean)/std
        │ pad_collate_xg            →  (B, T, 8) 变长 pad
        ▼
   DataLoader → model → MSE(x) → Adam + CosineAnnealing
```

---

## 2026-08-16 — `03_train_predictor` 如何预测 y1–y4（原理）

### 目标与定位

`python -m scripts_control.03_train_predictor` 训练一个**混合预测模型**：只用外生输入 x1–x8（8 维过程变量），预测目标 y1–y4（4 维）。这是**纯 x → y 前馈**——预测时不喂任何真实 y，也不做 y 的自回归反馈（无 teacher forcing）。

### 模型结构

`SS_NN_Hybrid(dim_u=8, dim_y=4, n_state=16, hidden=128, window=4)`，由两部分组成：

1. **线性状态空间基线 `LinearSSTorch`**：参数 `A∈R^{16×16}, B∈R^{16×8}, C∈R^{4×16}, D∈R^{4×8}`，用 N4SID 子空间辨识初始化。
2. **残差 MLP `ResidualMLP`**：滑窗（`window=4`）前馈网络，学习线性基线之上的非线性修正项。

### 预测流程（forward）

对标准化后的输入 `u`（shape `(B, T, 8)`），逐步得到 y1–y4 的预测：

1. **线性 rollout（只用 x）**——离散线性状态空间：
   ```
   x_{t+1} = A x_t + B u_t
   y_lin_t = C x_t + D u_t          # (B, T, 4) 线性基线
   ```
2. **拼接特征**——残差 MLP 每步的有效输入记为 `[u, y_lin]`（8 + 4 = 12 维真实信号）：
   - ① `u`（外生输入）、② `y_lin`（线性基线）。
   - 实现上还预留了第三个「上下文」槽位 `y_ctx`（16 = 8+4+4 维），本为 teacher forcing（真实 y）/自回归（上一步预测）预留；**纯 x 模式下 `y_prev=None` → 该槽位与 `y_lin` 重复、不携带新信息**，故有效输入等价于 `[u, y_lin]`。
3. **残差修正**：滑窗 4 步、展平（16×4 = 64 维）后过 `64→128→128→4` 的 MLP（GELU），得到 `y_res`。
4. **输出**：`y_pred = y_lin + y_res`（shape `(B, T, 4)`）。

关键点：`y_lin` 是 u 的纯函数，`y_prev=None` 时整条链路**只依赖 x**，没有任何 y 回流。

### 训练

- **初始化**：`fit_n4sid_on_train` 对训练集每条样本跑 `n4sid(u, y, order=16, n_lags=8)`，逐样本平均 A/B/C/D 作为线性 SS 初值。
- **优化**：AdamW（`lr=1e-3`, `weight_decay=1e-4`）+ cosine 退火（到 1e-5），early stopping `patience=30`。
- **损失**：
  - 主损失 `masked_mse(y_pred, Y, M)`：只在 `Y_mask=True`（有观测）的位置算 MSE。

### 推理（预测 y1–y4）

```python
y_pred = model(X, y_prev=None, teacher_forcing=0.0)   # (N, T, 4) 标准化
y_pred_denorm = y_pred * y_scale + y_mean              # 反标准化回原始空间
```

### 为什么"只看 x 就能预测 y"

- 线性部分本质是 N4SID 从训练数据 (u, y) 辨识出的**状态空间系统**：辨识完成后，输出 `y_lin` 完全由输入 u 驱动。
- 残差 MLP 在这个线性基线之上学一个**非线性修正**，捕捉线性模型遗漏的非线性/耦合。
- 两者相加即最终预测；整个过程无 y 反馈，符合"外生输入 → 目标"的因果设定。

### 相关代码

- 模型定义：`src_control/models/state_space_nn.py`（`SS_NN_Hybrid.forward` / `LinearSSTorch.rollout` / `ResidualMLP`）
- 训练/评估：`scripts_control/03_train_predictor.py`（`fit_n4sid_on_train`、训练循环、测试评估）

---

## 2026-08-16 — `train_y_poly` 多项式回归预测 Y 的多模式对比

### 目标

用中间目标 y1~y4（+ 可选 x1~x8 衍生特征）做多项式回归，拟合每个实验的最终结果 Y（标量），并对比不同特征模式的效果。

### 实验配置

- **模型管线**：`StandardScaler → PolynomialFeatures(degree 2/3) → Ridge`（`--alpha` 不指定时由 RidgeCV 自动选）。
- **数据**：171 个实验，Y 范围 [3028.86, 3715.53]。
- **切分**：全局模型按实验 7:1:2（train+val 训练 / test 评估）；per-group 每组内 80/20。
- **三种模式**：
  - `last`：y1~y4 最后有效值 → 4 特征；
  - `window`：y1~y4 最后 N 观测 → N×4 特征；
  - `per-group`：每个 y/x 列的 last/mean/delta，按组挑 top-8 特征 + 每组独立模型。

### 结果（按 RMSE 升序）

| degree | mode | RMSE | MAE | R² |
|---|---|---|---|---|
| 2 | per-group | **67.1** | **53.9** | 0.298 |
| 3 | window n=8 | 84.1 | 71.8 | **0.491** |
| 2 | last | 101.5 | 76.0 | 0.257 |
| 2 | last (drop-y4) | 108.2 | 81.1 | 0.157 |
| 2 | window n=4 | 113.2 | 82.6 | 0.076 |
| 2 | window n=8 | 115.1 | 77.8 | 0.046 |
| 3 | last | 116.1 | 82.8 | 0.028 |
| 3 | per-group | 249.2 | 92.2 | -8.677 |

### 小结

1. **per-group(deg2) RMSE 最低（67.1）**——「每组专属特征 + 独立模型」确实有效；但整体 R²(0.298) 反而低于 window n=8 deg3(0.491)，因为 R² 是 pooled 计算的、两组 test 划分口径不同，不宜直接跨模式比 R²。
2. **window n=8 + deg3 的 R² 最高(0.491)**，是全局模型里最均衡的选择——长窗口 + 三次项捕捉到更复杂的时序→终值关系。
3. **`--drop-y4` 反而更差**（0.257→0.157）——y4 信息有价值，不建议去掉。
4. **window deg2 比 last 更差**：特征维度从 4 涨到 16/32，但 RidgeCV 自动选的 α 过大（100~1000）导致欠拟合；只有升到 deg3 才真正发挥窗口优势。
5. **per-group deg3 崩溃**（G2 RMSE 达 524）：每组样本太少（约 20~34 训练样本）却塞进 56 个多项式特征，严重过拟合——分组模式不建议用三次项。

### 产物

- 对比脚本：`run_y_poly_compare.sh`（`bash run_y_poly_compare.sh` 一键跑 8 组合）
- 产物目录：`src/model_out_compare/`（pkl / metrics.json / predictions.csv / png，可按 `*_predictions.csv` 和 `*.png` 逐模式查看细节）

### 本次代码改动

- **`train_y_poly.py`**：修复 `RidgeCV(..., store_cv_values=True)` 参数在新版 scikit-learn(1.9.0) 已移除导致的直接崩溃（删掉该无用参数）；文件开头 docstring 补充各模式用法与区别。
- **`run_y_poly_compare.sh`**（新增）：顶部 `RUNS` 数组定义 8 个组合（可自行增删），循环运行 + 末尾自动汇总对比表；输出独立目录 `./src/model_out_compare/`（运行前自动清空，不影响 `src/model_out/` 历史产物）。
- **`src/README.md`**：新增「3.5 预测实验终值 Y（多项式回归）」章节（用法 + 模式区别 + 对比结论）。

---

## 2026-08-16 — `04_optimize` 优化未来 x3/x4/x6/x8 使 y4 最大（任务 4）

### 目标

README 任务 4：输入一段时间数据，优化未来 x3/x4/x6/x8，使 y4 最大（目标 = 仅 y4）。

### 模型结构

串联两个**冻结、可微**的 `SS_NN_Hybrid`（线性状态空间 + 残差 MLP，见 [state_space_nn.py](src_control/models/state_space_nn.py)）：

| 模型 | 类 / checkpoint | 作用 |
|---|---|---|
| x 预测器 | `SS_NN_Hybrid(dim_u=8, dim_y=8)` / `checkpoints/x_forecast_best.pt` | `forecast(x_ctx, H)` → 未来 x1–x8 默认轨迹（真自回归） |
| y4 预测器 | `SS_NN_Hybrid(dim_u=8, dim_y=4)` / `checkpoints/ss_nn_best.pt` | `forward(u)` → y1–y4，y4 = `output[..., 3]` 当奖励 |

其中每个 `SS_NN_Hybrid` = `LinearSSTorch`（`A/B/C/D`）+ `ResidualMLP`（滑窗前馈 MLP），输出 `y = y_lin + y_res`。初始化：x 预测器用 `init_x_recon`（线性 `D` 置单位阵、`x̂_lin≈u`），y4 预测器用 N4SID 子空间辨识。

### Pipeline（数据流）

```
过去窗口 x_ctx (C=32, 标准化)
  │
  ├─ ① x 预测器.forecast(x_ctx, H=48) ──► x_fut_default (H,8)   [不干预默认轨迹]
  │
  ├─ ② 决策变量 x_ctrl (H,4) = 默认轨迹的 [x3,x4,x6,x8] 列，requires_grad=True
  │      初值=预测值(clamp)；界=训练分布 [1%,99%] 分位数（默认 --bounds-mode train）
  │
  ├─ ③ 组装 future_x (H,8)：x_ctrl 覆盖第 2/3/5/7 列；x1/x2/x5/x7 固定=预测轨迹
  │      u = cat([x_ctx, future_x]) → (C+H, 8)
  │
  └─ ④ y4 预测器.forward(u) ──► y (1, C+H, 4)
         目标 = max mean( y[0, C:, 3] @观测采样相位 )      # H=48 覆盖一个日采样周期
         loss = -reward + λ·‖x_ctrl − x_default‖²          # λ = --effort-penalty
```

**关键实现点**（[optimizer.py](src_control/optimization/optimizer.py)）：

1. **决策/固定变量**：`DECISION_IDX=(2,3,5,7)`（x3/x4/x6/x8，可控）；`FIXED_IDX=(0,1,4,6)`（x1/x2/x5/x7，随预测走、不优化）。
2. **优化器**：Adam 投影梯度上升 + 多起点（起点 0 = 预测默认值 clamp，其余界内随机），每步 `clamp_` 投影到标准化界；`no-regression` 保证（初值 = 可行点，结果不会差于 baseline）。
3. **搜索界**：默认用训练集 x3/x4/x6/x8 的分位数界（`--bounds-mode train --bound-quantile 0.01` = 1%-99%），替代宽松物理界，防止代理模型外推虚高；物理界 → 标准化界 `lo_norm=(lo_raw−mean)/scale`（[standardized_bounds](src_control/optimization/optimizer.py#L53)）。
4. **reward 对齐采样相位**：y4 是「日采样」目标（约每 48 步 = 24h 测一次），默认 `--horizon 48` 覆盖一个完整采样周期；reward = 观测相位上的 y4 均值（`y4_phase_mask`，只传相位不传值），无观测点回退末点。
5. **effort 正则**（可选）：`loss = -reward + λ·‖x_ctrl − x_default‖²`，λ 越大越贴近默认轨迹，抑制跳变与外推。
6. **梯度流**：`_assemble_future` 用 `torch.cat` 逐列拼接（非 in-place），保证梯度只回传 `x_ctrl`。

### 关键结论

1. 目标稳定上升、无退化（`no-regression` 保证）。
2. **对齐采样相位 + 分布内硬约束，把外推虚高从 +84% 收敛到 +32%**（样本 240116，train 1%-99% 界）；**硬约束比 `--effort-penalty` 有效**（effort 0.1→1.0 只压到 +23%，收紧界直接到 +16.6%）。
3. **残留「贴边」**：y4 预测器对 x 的响应接近单调，只要界非零宽，最大化 y4 就会贴到界边缘；剩下的是「真实可实现的提升」还是「模型在分布边缘的偏差」，需留出集 counterfactual 验证才能区分。
4. 默认 `--bounds-mode train --bound-quantile 0.01` 是合理中间选择；更保守可用 `--bound-quantile 0.05`。

### 产物

- `src_control/optimization/optimizer.py`：`maximize_y4`（优化主循环）/ `_assemble_future`（梯度流组装）/ `standardized_bounds`（界换算）
- `scripts_control/04_optimize.py`：加载两个 ckpt + `scalers.npz`，取 test 样本 context，调 `maximize_y4`，反标准化 + 画图 + 汇总
- 输出：`results/optimization/optimize_metrics.json` + `optimized_vs_baseline_{i}.png`

---

## 2026-08-16 — `run_all.sh`：4 项任务最优模型总流程（一键复现）

### 目标

把本日志前述 4 个实验模块的**最优模型**固化成一条可一键执行的编排脚本
`run_all.sh`（仓库根目录），按依赖顺序完成「数据预处理 → 预测 → 优化」全流程；
脚本头部内置每项任务的方法说明与运行用法（`bash run_all.sh --help` 可查看）。

### 4 项任务 → 最优模型

| # | 任务（模块） | 最优模型 | 依据 |
|---|---|---|---|
| 1 | 多组 x1–x8 预测对比 | **PathInt shared** | RMSE 1372.5；PathInt 优于 LSTM 12.4% |
| 2 | 预测 y1–y4 | **SS_NN_Hybrid**（`03_train_predictor`） | y4 R²=0.987；纯 x→y 前馈 |
| 3 | 预测实验终值 Y | **per-group deg2**（`train_y_poly`） | RMSE 67.1 最低 |
| 4 | 优化 x3/x4/x6/x8 使 y4 最大 | **04_optimize**（train 1%–99% 界 + 相位对齐） | 收敛外推虚高 |

### 实验方法（脚本内附，与 `run_all.sh` 头部注释一致）

1. **任务1（多组预测对比）**：`split_experiments_groupwise` 按实验 ID 分组切分 + `Scaler` 8 维标准化；
   LSTM(hidden=128×2, dropout=0.1, lr=2e-3) vs PathInt(dim_state=128, hidden=128, lr=5e-4)，
   组策略 shared/group_head(FiLM 头 γ,β∈R^8)；`epochs=60`，前 10 epoch Teacher Forcing 后纯自回归
   rollout，`loss=MSE(x)` → **PathInt shared 最优**。
2. **任务2（y1–y4 预测）**：`SS_NN_Hybrid` = N4SID 初始化的线性状态空间基线（A∈R16×16,B∈R16×8,
   C∈R4×16,D∈R4×8）+ 残差 MLP(window=4, 64→128→128→4)；纯 x→y 前馈
   （`x_{t+1}=Ax_t+Bu_t, y_lin=Cx_t+Du_t` → `y_pred=y_lin+y_res`），无 teacher forcing、无 y 自回归；
   AdamW(lr=1e-3,wd=1e-4)+cosine 退火+early stop(patience=30)，`masked_mse` 仅算观测位。
3. **任务3（Y 终值预测）**：`StandardScaler → PolynomialFeatures(2) → Ridge`(RidgeCV 选 α)；
   每组从 36 个 last/mean/delta 候选(y1–y4+x1–x8)按组内 Pearson\|r\| 挑 top-8，每组独立 Ridge，
   组内 80/20 划分。
4. **任务4（优化）**：冻结 `x_forecast`(dim_y=8) 给默认轨迹、`ss_nn`(dim_y=4) 当奖励；
   仅可控量 x3/x4/x6/x8(`DECISION_IDX=2,3,5,7`) 为搜索变量，投影梯度上升
   `loss=-reward+λ‖x_ctrl−x_default‖²`；采样相位对齐(H=48 覆盖 24h 采样) + 分布内约束
   (train 1%–99% 分位数界)。

### 流程结构

```
preprocess ──► [任务1] compare_multigroup（PathInt shared）
           ──► [任务2] 03_train_predictor（SS_NN_Hybrid）
           ──► [任务3] train_y_poly --degree 2 --per-group
           ──► [任务4] 08_train_x_model（前置）→ 04_optimize
```

### 用法

```bash
bash run_all.sh --skip-train --device cpu   # 复用现有 ckpt，只跑预测/评估/优化
bash run_all.sh                             # 全量复现（默认 cuda，训练耗时较长）
bash run_all.sh --help                      # 查看任务对照表 + 方法 + 用法
```

### 实现说明

- **任务4 前置**：`04_optimize` 硬编码加载 `x_forecast_best.pt`，因此 `08_train_x_model`
  虽不在四项内，仍作为 4a 步骤纳入总流程，缺失会直接报错。
- **`--skip-train`**：跳过 3 个耗时训练（任务1 对比训练、任务2 的 03、任务4 前置的 08），
  只跑预测/评估/优化（复用现有 checkpoint）。
- **路径坑**：`preprocess.py` 默认 `--root` 是旧 NAS 路径、`compare_multigroup.py` 默认
  `--base-dir` 是 Windows 路径，脚本里均显式传 `"$ROOT"`（脚本所在仓库根目录）。
- **过时文档**：`compare_multigroup.py` docstring 里的 `--predict`/`--predict-model` 开关在
  真实 argparse 中不存在，脚本以 `--skip-train`（复用 ckpt 重评+重画）作为任务1 的预测/评估路径。

### 产物

- 任务1：`src/model_out/compare_metrics.json` + `compare_bars_*.png`
- 任务2：`checkpoints/ss_nn_best.pt`、`results/metrics/test_metrics.json`、`results/predictions/test_predictions.npz`
- 任务3：`src/model_out/y_poly_deg2_engineered_pergroup.{pkl,metrics.json,predictions.csv,png}`
- 任务4：`checkpoints/x_forecast_best.pt`、`results/optimization/optimize_metrics.json` + 优化对比 png
