# 实验任务输出对照表

> 本文档依据《实验日志.md》整理，用于快速查询：**每个任务的输出图片放在哪里、绘制该图所需的数据（CSV / npz / json / ckpt）在哪里**。
> 整理时间：2026-08-16。

---

## 一、数据来源总览（上游）

| 层级 | 路径 | 说明 |
|---|---|---|
| 原始数据 | `1/ 2/ 3/ 4/ 5/` | 171 个 CSV（36+36+32+43+24），每文件含 x1–x8、稀疏 y1–y4、终值 Y |
| 预处理中间数据 | `data/processed/{aligned_dataset,train,test,scalers}.npz` | `src_control/preprocess.py` 产出（对齐/清洗/标准化，seq-len 320） |
| y 模型权重 | `checkpoints/ss_nn_best.pt` / `ss_nn_last.pt` | 任务 2（`03_train_predictor.py`）产出 |
| x 外推模型权重 | `checkpoints/x_forecast_best.pt` / `x_forecast_last.pt` | 任务 4 前置（`08_train_x_model.py`）产出 |
| 多组对比模型权重 | `src/model_out/forecaster_{lstm,pathint}_{shared,group_head}.pt` + `scalers*.npz` | 任务 1（`compare_multigroup.py`）产出 |

---

## 二、各任务输出图片 ↔ 数据来源（总表）

### 任务 1 — 多组 x1–x8 预测对比（`compare_multigroup.py` / `visualize.py`）

| 输出图片 | 绘图所需数据 |
|---|---|
| `src/model_out/compare_bars_lstm.png`<br>`compare_bars_pathint.png`<br>`compare_bars_all.png`<br>`compare_bars_all_train.png`<br>`compare_bars_tf_vs_ar.png` | `src/model_out/compare_metrics.json`（test）/ `compare_metrics_train.json` / `compare_metrics_tf.json` —— 由 `forecaster_*.pt` + `scalers*.npz` + 原始 CSV（test/train 集）算出 |
| `src/analysis_out/forecast_x1_x8_{backbone}_{mode}.png` | `src/model_out/forecaster_*.pt` + `scalers*.npz` + 原始 CSV 测试集 |
| `src/analysis_out/error_per_dim_{backbone}_{mode}.png` | 同上（分维度 RMSE） |
| `src/analysis_out/compare_predictions_x1/x5/x6/x7/x8.png` | `forecaster_*.pt` + `scalers*.npz` + 原始 CSV |

### 任务 2 — 预测 y1–y4（`03_train_predictor.py` → `06_visualize.py`）

| 输出图片 | 绘图所需数据 |
|---|---|
| `src_control/analysis_out/forecast_y1_y4.png` | `checkpoints/ss_nn_best.pt` + `data/processed/scalers.npz` + `data/processed/test.npz` |
| `src_control/analysis_out/forecast_x1_x8.png` | `checkpoints/x_forecast_best.pt` + `scalers.npz` + `test.npz` |
| `src_control/analysis_out/error_distribution.png` | 上述模型预测 + 真实值的残差（`results/predictions/test_predictions.npz`） |

### 任务 3 — 预测终值 Y（`train_y_poly.py` / `run_y_poly_compare.sh`）

| 输出图片 | 绘图所需数据 |
|---|---|
| `src/model_out_compare/y_poly_deg2/deg3_*.png`（8 张，一键对比） | 原始 CSV（dirs 1–5）提取 y1–y4/x1–x8 特征 + 终值 Y；散点数据 = 同目录 `*_predictions.csv`，指标 = `*_metrics.json` |
| `src/model_out/y_poly_deg2*.png` / `y_poly_deg3*.png`（旧目录，更多变体） | 同上，数据在同目录 `y_poly_*_predictions.csv` / `*_metrics.json`；模型 `*.pkl`，系数 `*_coefficients.csv` |

> `src/model_out_compare/` = `run_y_poly_compare.sh` 一键对比的 8 组合产物；`src/model_out/` = 历史单跑产物。

### 任务 4 — 优化 x3/x4/x6/x8 使 y4 最大（`04_optimize.py`）

| 输出图片 | 绘图所需数据 |
|---|---|
| `results/optimization/optimized_vs_baseline_0.png` / `_1.png` | `checkpoints/x_forecast_best.pt` + `checkpoints/ss_nn_best.pt` + `data/processed/scalers.npz` + `test.npz` |
| `results/optimization/optimize_metrics.json` | 每样本 baseline/optimized y4、提升幅度、是否越界（图的汇总数据） |

### 附加 — 关联性分析（`src/analyze.py` → `src/analysis_out/`）

| 输出图片 | 绘图所需数据 |
|---|---|
| `correlation_matrix*.png` / `x_y_correlation.png` / `x_last_vs_Y.png` / `Y_correlation_matrix*.png` / `Y_top10_bar*.png` | 原始 CSV（dirs 1–5），配套 CSV 在同目录（`summary_stats.csv`、`correlation_matrix*.csv`、`x_y_correlation.csv`、`y_correlation.csv`、`feature_vs_Y_importance.csv`） |

### 附加 — PPT 图（`07_ppt_figures.py`）

| 输出图片 | 绘图所需数据 |
|---|---|
| `src_control/analysis_out/ppt/01..08_*.png`（8 张中文标注图） | `results/predictions/test_predictions.npz` + 数据（当前仓库 `ppt/` 只有 `outline.md`，这 8 张图尚未生成） |

---

## 三、图片 ↔ CSV 精确对照

**结论：不是每张图都有 CSV**。数据来源分三类：

| 类别 | 说明 | 涉及脚本 |
|---|---|---|
| **A. 原始 CSV 直读** | 图片直接由 `1/..5/` 的 171 个 CSV 画出（`load_all` 读 `{base_dir}/{组}/*.csv`） | `src/analyze.py`、`src/compare_multigroup.py`、`src/visualize.py`、`src/train_y_poly.py` |
| **B. 中间 CSV 持久化** | 脚本同时写出 CSV 和 PNG，CSV 就是图的数据（可据此重画） | `src/analyze.py`、`src/train_y_poly.py` |
| **C. 无 CSV（npz/json/ckpt）** | `src_control/` 整套走预处理 npz，不碰 CSV | `scripts_control/03/04/06/08` |

### 3.1 `src/analyze.py` → `src/analysis_out/`（图 ↔ CSV 一一对应）

| 输出图片 | 对应 CSV（同目录 `src/analysis_out/`） | 关系 |
|---|---|---|
| `correlation_matrix.png` | `correlation_matrix.csv` | CSV 是 x1–x8 相关矩阵，图即其热图 |
| `correlation_matrix_group_{1..5}.png` | `correlation_matrix_group_{1..5}.csv` | 同上，按组 |
| `x_y_correlation.png` | `x_y_correlation.csv` | x↔y 交叉相关热图 |
| `Y_top10_bar.png` | `Y_correlation_matrix.csv` | 取与 Y 相关前 10 画柱状图 |
| `Y_top10_bar_group_{1..5}.png` | `Y_correlation_matrix_group_{1..5}.csv` | 同上，按组 |
| `x_last_vs_Y.png` | **无 CSV** | 直接用原始 CSV 的「末态 x vs Y」画散点，未落盘 CSV |

另有 3 个 CSV **不是图的数据**，是统计表：`summary_stats.csv`、`feature_vs_Y_importance.csv`、`y_correlation.csv`。

### 3.2 `src/train_y_poly.py` → `src/model_out_compare/`（及旧的 `src/model_out/`）

| 输出图片 | 对应 CSV（同目录，`y_poly_{tag}_*`） | 关系 |
|---|---|---|
| `y_poly_{tag}.png` | `y_poly_{tag}_predictions.csv` | 图是「真实 Y vs 预测 Y」散点+残差，CSV 是同一份逐样本数据（`Y_true/Y_pred`） |
| —（无图） | `y_poly_{tag}_coefficients.csv` | 多项式系数表，非图数据 |
| —（无图） | `y_poly_{tag}_metrics.json` | RMSE/MAE/R² 指标，非图数据 |

### 3.3 `src/compare_multigroup.py` → `src/model_out/`

| 输出图片 | 对应数据文件 |
|---|---|
| `compare_bars_lstm.png` / `compare_bars_pathint.png` / `compare_bars_all.png` / `compare_bars_all_train.png` / `compare_bars_tf_vs_ar.png` | **无 CSV**，对应 `compare_metrics.json` / `compare_metrics_train.json` / `compare_metrics_tf.json`（同目录）；上游原始来源 = `1/..5/*.csv` + `forecaster_*.pt` |

### 3.4 `src/visualize.py` → `src/analysis_out/`

| 输出图片 | 对应数据文件 |
|---|---|
| `forecast_x1_x8_{backbone}_{mode}.png` | **无 CSV**，直接读 `1/..5/*.csv`（`load_all`）+ `src/model_out/forecaster_*.pt` |
| `error_per_dim_{backbone}_{mode}.png` | 同上 |
| `compare_predictions_x{1,5,6,7,8}.png` | 同上（`src/compare_predictions.py` 产） |

### 3.5 `src_control/` 现代控制管道（**全部无 CSV，走 npz**）

| 输出图片 | 对应数据文件 |
|---|---|
| `src_control/analysis_out/forecast_y1_y4.png` | `checkpoints/ss_nn_best.pt` + `data/processed/scalers.npz` + `data/processed/test.npz` |
| `src_control/analysis_out/forecast_x1_x8.png` | `checkpoints/x_forecast_best.pt` + `data/processed/scalers.npz` + `test.npz` |
| `src_control/analysis_out/error_distribution.png` | 上述模型预测 + 真值残差（`results/predictions/test_predictions.npz`） |
| `results/optimization/optimized_vs_baseline_{i}.png` | `checkpoints/x_forecast_best.pt` + `checkpoints/ss_nn_best.pt` + `data/processed/{scalers,test}.npz`；同目录 `optimize_metrics.json` 是汇总表 |

---

## 四、一句话速记

- **有 CSV 直接对应的图**：`analyze.py` 的 `correlation_matrix*.png`、`x_y_correlation.png`、`Y_top10_bar*.png`（对应同名前缀的 `.csv`）；`train_y_poly.py` 的 `y_poly_*.png`（对应 `y_poly_*_predictions.csv`）。
- **没有 CSV、但有 JSON/npz 对应**：`compare_bars_*.png` ← `compare_metrics*.json`；`src_control` 与 `04_optimize` 的图 ← `data/processed/*.npz` + `.pt`。
- **连落盘中间数据都没有、纯靠原始 CSV 现算现画**：`visualize.py` 的 `forecast_x1_x8_*`、`error_per_dim_*`、`x_last_vs_Y.png`。

---

## 五、实际存在 vs 文档中待生成

| 项目 | 状态 |
|---|---|
| `results/figures/`（`src_control.analysis.correlation` 输出：correlation_heatmap / mi_heatmap / pca_scree / lag_x_to_y4 / granger_xy） | README 有文档，**当前仓库未生成** |
| `src_control/analysis_out/ppt/`（`07_ppt_figures.py` 的 8 张图） | 仅 `ppt/outline.md`，**图未生成** |

---

## 六、一键复现入口

完整流程见 [`run_all.sh`](run_all.sh)：预处理 → 任务1（多组对比）→ 任务2（预测 y1–y4）→ 任务3（预测终值 Y）→ 任务4（优化 y4）。每个任务的产物路径见脚本末尾注释。
